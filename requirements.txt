# Core runtime -- this is what gets packaged into the Lambda.
#
# Keep the UI's dependencies OUT of this file. Streamlit's tree (pyarrow,
# pandas, numpy, altair, pydeck, PIL) is ~260MB unzipped on its own, which
# blows past Lambda's 250MB limit and fails the deploy. UI deps live in
# requirements-ui.txt.
psycopg[binary]>=3.2.0
boto3>=1.35.0
python-dotenv>=1.0.0
